{
  "entities": {
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A message sent by a user in the community chat.",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The content of the message."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who sent the message."
        },
        "userName": {
          "type": "string",
          "description": "The display name of the user."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp when the message was created."
        }
      },
      "required": ["text", "userId", "userName", "timestamp"]
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Collection of global community chat messages."
    }
  }
}
